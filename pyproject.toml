[project]
name = "appimage-handler"
version = "0.1.0"
description = "CLI tool handling .AppImage updates and potentially extraction or desktop integration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "packaging>=25.0",
    "requests>=2.32.5",
    "typer>=0.20.0",
    "rich>=14.2.0",
]
